<p></p>
<p><%= link_to 'Voltar', admin_pages_path, class: 'btn btn-primary' %></p>
<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-heading">Páginas <small>(edição)</small></div>
      <div class="panel-body">
        <%= render 'form' %>
      </div>
    </div>
  </div>
</div>

<% content_for :title_breadcrumb do %>
  <section class="content-header">
    <h1>
      Páginas
      <small>(edição)</small>
    </h1>
    <%= render 'breadcrumb' %>
  </section>
<% end %>
