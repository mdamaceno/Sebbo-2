<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-body">
        <table class="datatable table table-hover display">
          <thead>
            <tr>
              <th width="60">Código</th>
              <th>Forma de pagamento</th>
              <th>Vendedor</th>
              <th width="140">Status</th>
              <th width="10"></th>
              <th width="10"></th>
              <th width="10"></th>
            </tr>
          </thead>
        
          <tbody>
            <% @orders.each do |order| %>
              <tr>
                <td><%= order.id %></td>
                <td><%= order.payment_method %></td>
                <td><%= order.user.email %></td>
                <td><%= order.status %></td>
                <td><%= link_to 'Ver', admin_order_path(order) %></td>
                <td><%= link_to 'Editar', admin_edit_order_path(order) %></td>
                <td><%= link_to 'Excluir', admin_order_path(order), method: :delete, data: { confirm: 'Deseja mesmo excluir?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
