<div class="col-md-12">
  <%= simple_form_for([:admin, @discount]) do |f| %>
    <% if @discount.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@discount.errors.count, "error") %> prohibited this page from being saved:</h2>

        <ul>
        <% @discount.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="row">
      <div class="col-md-3">
        <div class="form-group">
          <%= f.input :code, label: 'Código', input_html: { class: 'form-control', value: "#{generate_code}" } %>
        </div>
      </div>
    </div>
    <div class="form-group">
      <%= f.input :value, label: 'Valor', input_html: { class: 'form-control' } %>
    </div>
    <div class="form-group">
      <%= f.input :type_d, label: 'Tipo de desconto', collection: Discount::TYPES, as: :radio_buttons %>
    </div>
    <%= f.button :submit, 'Salvar', class: 'btn-primary' %>
  <% end %>
</div>
