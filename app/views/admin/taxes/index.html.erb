<p id="notice"><%= notice %></p>

<p><%= link_to 'Novo imposto', admin_new_tax_path, class: 'btn btn-primary' %></p>

<p>Os impostos aqui registrados incidem sobre cada livro registrado no sistema.</p>

<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-body">
        <table class="datatable table table-hover display">
          <thead>
            <tr>
              <th width="10">ID</th>
              <th>Nome</th>
              <th width="20">Porcentagem</th>
              <th width="10"></th>
              <th width="10"></th>
            </tr>
          </thead>
        
          <tbody>
            <% @taxes.each do |tax| %>
              <tr>
                <td><%= tax.id %></td>
                <td><%= tax.name %></td>
                <td><%= tax.percentage %>%</td>
                <td><%= link_to 'Editar', admin_edit_tax_path(tax), class: 'btn btn-xs btn-primary' %></td>
                <td><%= link_to 'Excluir', admin_tax_path(tax), class: 'btn btn-xs btn-danger', method: :delete, data: { confirm: 'Tem certeza que quer excluir?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<% content_for :title_breadcrumb do %>
  <section class="content-header">
    <h1>
      Impostos
      <small>Listagem de impostos</small>
    </h1>
    <%= render 'breadcrumb' %>
  </section>
<% end %>
