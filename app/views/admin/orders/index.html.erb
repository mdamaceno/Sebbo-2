<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-body">
        <table class="datatable table table-hover display">
          <thead>
            <tr>
              <th width="60">Código</th>
              <th>Email</th>
              <th>Vendedor</th>
              <th width="140">Status</th>
              <th width="10"></th>
              <th width="10"></th>
              <th width="10"></th>
            </tr>
          </thead>
        
          <tbody>
            <% @orders.each do |order| %>
              <tr>
                <td><%= "#{order.id}".rjust(8, '0') %></td>
                <td><%= order.user.email %></td>
                <td><%= "#{order.user.firstname} #{order.user.lastname}" %></td>
                <td><%= order.status %></td>
                <td><%= link_to 'Ver', admin_order_path(order), class: 'btn btn-primary btn-xs' %></td>
                <td><%= link_to 'Editar', admin_edit_order_path(order), class: 'btn btn-primary btn-xs' %></td>
                <td><%= link_to 'Cancelar', admin_order_path(order), class: 'btn btn-danger btn-xs', method: :delete, data: { confirm: 'Deseja mesmo cancelar este pedido?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
